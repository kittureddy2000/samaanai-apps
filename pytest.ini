[pytest]
DJANGO_SETTINGS_MODULE = samaanai.test_settings
python_files = test_*.py
# Remove any --no-migrations flag
# Add explicit flag to ensure migrations run
addopts = --create-db
filterwarnings =
    ignore::DeprecationWarning
markers =
    requires_dateutil: mark tests that require python-dateutil
    playwright: mark tests requiring browser